Track UI expectations from user:
- Checkout right-side cart summary should display images.
- After clicking order button, user should see their own orders.
- Orders page should allow user to cancel/delete their own orders.
- Order button should be protected from weird behavior (handled by disable/loading already).

