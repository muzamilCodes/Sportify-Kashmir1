PORT=4000
NODE_ENV=production
MONGO_URI=
SECRET_KEY=

# Live frontend origin(s), comma-separated when more than one
FRONTEND_URL=https://sportify-kashmir1.vercel.app
FRONTEND_URLS=

# Use either SMTP or an HTTP email provider.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
# SMTP_SECURE=false for port 587; true for port 465
SMTP_SECURE=false

# Alternative providers (recommended for hosted deployments)
BREVO_API_KEY=
RESEND_API_KEY=
